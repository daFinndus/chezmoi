- Network widget (Bandwidth per application (maybe only the biggest ones), QR code for network, Captive Portal)
- VPN widget (Display connected VPN, used interface and ip, speed, endpoint)
- Threat indicator (show failed ssh attempts and status, new sockets, apparmor, firewall status, listening ports, established connectsions, disk encryption, secure boot, kernel lockdown, failed services)
- Distinguish updates, security, misc, etc. (arch audit)
- Battery widget (estimated time, percentage, etc.)

- Make everything else on this system react to Themes.background, .shade, the border properties and so on

# Theme System

## Theme data

- [ ] Decide theme directory location
- [ ] Create `themes/`
- [ ] Create/migrate `themes.json` with all theme definitions
- [ ] Create `active.json` containing the currently active theme
- [ ] Define the theme override structure
- [ ] Define theme value precedence:
  - [ ] Theme definition
  - [ ] Active theme
  - [ ] Overrides
  - [ ] Effective theme

## Theme application

- [ ] Create `apply-theme.sh`
- [ ] Read active theme and overrides
- [ ] Generate Hyprland configuration
- [ ] Generate Dunst configuration
- [ ] Generate Rofi configuration
- [ ] Generate VS Code configuration
- [ ] Generate Obsidian configuration
- [ ] Reload/restart affected applications
- [ ] Keep generated configurations separate from chezmoi-managed files

## Quickshell / IPC

- [ ] Create `ThemeManager` singleton
- [ ] Load `themes.json`
- [ ] Load `active.json`
- [ ] Expose effective theme values to Quickshell
- [ ] Add `theme.get` IPC handler
- [ ] Add `theme.set` IPC handler
- [ ] Add `theme.override` IPC handler
- [ ] Add `theme.reset-overrides` IPC handler
- [ ] Write theme changes to `active.json`
- [ ] Run `apply-theme.sh` after changes
- [ ] Watch `active.json` for external changes

## Integration

- [ ] Add theme files/scripts to chezmoi
- [ ] Convert application configurations to consume generated theme data
- [ ] Ensure theme changes do not modify chezmoi-managed source files
- [ ] Test changing themes from Quickshell
- [ ] Test changing themes through IPC
- [ ] Test external changes to `active.json`
- [ ] Test a fresh chezmoi deployment

- Add color pick
- Infinitely scrolling wallpaper

- Make dotfiles (available for all usernames, structures, etc.)
- Create installscript (for fresh devices, with dependencies, etc.)

failed SSH authentication attempts
newly opened listening ports
failed systemd services
AppArmor/LSM denials
unexpected privilege escalation events
new network interfaces
firewall state changes
Secure Boot / kernel lockdown state
unusual authentication activity

!  4 failed SSH attempts
   2 min ago

!  New listening socket
   0.0.0.0:8080
   node

!  AppArmor denial
   firefox
   11:42
